import './globals.css'; import Link from 'next/link';
const nav=['events','calendar','taxa','evidence','routes','lead-packages','settings'];
export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="en"><body><div className="border-b border-line bg-navy/95"><div className="mx-auto flex max-w-7xl items-center justify-between p-4"><Link href="/" className="text-xl font-bold text-amber">ExpoWatch</Link><nav className="flex flex-wrap gap-3 text-sm">{nav.map(n=><Link key={n} href={`/${n}`} className="hover:text-amber">{n}</Link>)}</nav></div></div><main className="mx-auto max-w-7xl p-4">{children}</main><footer className="mt-10 border-t border-line p-4 text-center text-sm text-slate-400">Internal use only. Intelligence support, not proof of criminality.</footer></body></html>}
