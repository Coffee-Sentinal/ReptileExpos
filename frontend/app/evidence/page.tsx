import {api} from '@/lib/api'; export default async function Evidence(){const items=await api.evidence(); return <><h1 className="text-3xl font-bold">Evidence registry</h1><div className="card my-4"><h2 className="font-bold text-amber">Manual evidence-entry form</h2><p className="text-slate-300">MVP API supports POST /api/evidence. File uploads, hashing automation, and chain-of-custody workflows are future work.</p></div><div className="grid-auto">{items.map((x:any)=><div className="card" key={x.id}><h2 className="font-bold">{x.title}</h2><p className="text-amber">{x.evidence_type} · confidence {x.confidence}</p><p>{x.summary}</p><p className="text-sm text-slate-400">Observed {x.date_observed}; red flags: {x.extracted_red_flags.join(', ')}</p></div>)}</div></>}
