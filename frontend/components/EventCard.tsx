import Link from 'next/link'; import {RiskBadge} from './RiskBadge'; import type {Event} from '@/lib/api';
export function EventCard({event}:{event:Event}){const d=event.dates?.[0]; return <div className="card"><div className="flex items-start justify-between gap-3"><h3 className="font-bold text-lg">{event.name}</h3><RiskBadge priority={event.risk_score?.priority}/></div><p className="text-slate-300">{event.venue.city}, {event.venue.country} · {event.venue.name}</p><p className="text-sm text-slate-400">Next known date: {d?.start_date} to {d?.end_date}</p><p className="mt-2 text-sm">Confidence level: {event.confidence}</p><Link className="mt-3 inline-block text-amber" href={`/events/${event.slug}`}>Open detail →</Link></div>}
