import type {RiskScore} from '@/lib/types'; import RiskBadge from './RiskBadge';
export default function RiskExplanationPanel({score}:{score:RiskScore}){return <section className="card"><div className="flex items-center justify-between"><h2 className="section-title">Why flagged?</h2><RiskBadge level={score.monitoringPriority}/></div><p className="mt-2 text-3xl font-black">{score.overallScore}<span className="text-base text-slate-400">/100</span></p><div className="mt-3 grid gap-2 md:grid-cols-3 text-sm"><span>Species {score.speciesRisk}</span><span>Online {score.onlineSignalRisk}</span><span>Routes {Math.max(score.inboundRouteRisk,score.outboundRouteRisk)}</span><span>Courier {score.courierLinkageRisk}</span><span>Reports {score.seizureReportRisk}</span><span>Paperwork {score.paperworkRisk}</span></div><ul className="mt-4 list-disc space-y-1 pl-5 text-sm text-slate-300">{score.explanation.map(x=><li key={x}>{x}</li>)}</ul></section>}
