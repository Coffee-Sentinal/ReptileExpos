import {api} from '@/lib/api'; export default async function Routes(){const routes=await api.routes(); return <><h1 className="text-3xl font-bold">Route-risk explorer</h1><p className="mb-4 text-slate-300">Mock route-risk intersections for authorized verification only; not proof of criminality.</p><div className="grid-auto">{routes.map((r:any)=><div className="card" key={r.id}><h2 className="font-bold text-amber">{r.direction}: {r.origin} → {r.destination}</h2><p>{r.window_start} to {r.window_end}</p><p>Transit hubs: {r.transit_hubs.join(' → ')}</p><p className="text-sm text-slate-300">{r.rationale}</p><p>Confidence level: {r.confidence}</p></div>)}</div></>}
