import EvidenceCard from '@/components/EvidenceCard'; import {allEvidence,allEvents,allTaxa} from '@/lib/data';
export default function Evidence(){return <><h1 className="text-4xl font-black">Evidence registry</h1><p className="mb-5 mt-2 text-slate-300">Static public-safe evidence examples only. No file uploads, private scraping, or sensitive personal data.</p><section className="card mb-6 grid gap-3 md:grid-cols-4"><input className="input" placeholder="Search title or summary"/><select className="input"><option>Evidence type</option></select><select className="input"><option>Confidence level</option></select><select className="input"><option>Public-safe status</option></select></section><div className="grid gap-4 md:grid-cols-2">{allEvidence.map(e=><div key={e.id}><EvidenceCard item={e}/><p className="mt-1 text-xs text-slate-500">Related event: {allEvents.find(x=>x.id===e.relatedEventId)?.name}; taxa: {e.relatedTaxaIds.map(id=>allTaxa.find(t=>t.id===id)?.scientificName).filter(Boolean).join(', ')}</p></div>)}</div></>}
