import {api} from '@/lib/api'; export default async function Taxa(){const taxa=await api.taxa(); return <><h1 className="text-3xl font-bold">Proxy taxa library</h1><div className="mt-4 grid-auto">{taxa.map((t:any)=><div className="card" key={t.id}><h2 className="font-bold text-amber">{t.scientific_name}</h2><p>{t.common_name} · {t.taxon_group}</p><p>CITES {t.cites_appendix}; EU {t.eu_annex}</p><p className="text-sm text-slate-300">Possible origin countries: {t.origin_countries.join(', ')}</p><p className="text-sm">{t.id_notes}</p></div>)}</div></>}
