import {api} from '@/lib/api'; export default async function Leads(){const rows=await api.leadPackages(); return <><h1 className="text-3xl font-bold">Saved lead package previews</h1><div className="mt-4 grid-auto">{rows.map((p:any)=><div className="card" key={p.id}><h2 className="font-bold text-amber">{p.title}</h2><p>{p.caveat}</p><div className="text-sm text-slate-300" dangerouslySetInnerHTML={{__html:p.html}}/></div>)}</div></>}
