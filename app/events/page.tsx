import EventCard from '@/components/EventCard'; import {allEvents,websitesFor,eventTaxaFor,evidenceFor,routesFor} from '@/lib/data';
export default function Events(){const countries=[...new Set(allEvents.map(e=>e.venue.country))]; return <><h1 className="mb-2 text-4xl font-black">Event registry</h1><p className="mb-5 text-slate-300">Searchable/filterable public-safe registry. Filters are static UI controls for GitHub Pages and can be extended client-side.</p><section className="card mb-6 grid gap-3 md:grid-cols-4"><input className="input" placeholder="Search event, city, venue"/><select className="input"><option>Country</option>{countries.map(c=><option key={c}>{c}</option>)}</select><select className="input"><option>Monitoring priority</option><option>low</option><option>medium</option><option>high</option><option>critical</option></select><select className="input"><option>Active monitoring window</option><option>Has high-risk proxy taxa</option><option>Has linked online advert source</option><option>Has route-risk corridors</option></select></section><div className="grid gap-4 md:grid-cols-2">{allEvents.map(e=><EventCard key={e.id} event={e} websites={websitesFor(e.id)} proxyTaxaCount={eventTaxaFor(e.id).length} evidenceCount={evidenceFor(e.id).length}/>)}</div></>}
