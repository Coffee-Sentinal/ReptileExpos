import type {EvidenceItem} from '@/lib/types'; import ConfidenceBadge from './ConfidenceBadge';
export default function EvidenceCard({item}:{item:EvidenceItem}){return <article className="card"><div className="flex justify-between gap-3"><h3 className="font-bold">{item.title}</h3><ConfidenceBadge level={item.confidence}/></div><p className="mt-1 text-sm text-amber">{item.evidenceType} · {item.publicSafeStatus}</p><p className="mt-2 text-sm text-slate-300">{item.summary}</p><p className="mt-2 text-xs text-slate-400">Observed {item.dateObserved}{item.datePosted?` · posted ${item.datePosted}`:''}</p><div className="mt-2 flex flex-wrap gap-2">{item.extractedRedFlags.map(f=><span className="rounded bg-danger/10 px-2 py-1 text-xs text-red-200" key={f}>{f}</span>)}</div></article>}
