'use client';
import Link from 'next/link'; import {MapContainer,TileLayer,CircleMarker,Popup} from 'react-leaflet'; import type {Event} from '@/lib/types';
const colors={low:'#68f0b0',medium:'#f6c453',high:'#ff6b6b',critical:'#e879f9'};
export default function MapDashboard({events}:{events:Event[]}){return <MapContainer center={[25,5]} zoom={2} scrollWheelZoom={false} className="min-h-[520px] rounded-2xl"><TileLayer attribution="&copy; OpenStreetMap contributors" url="https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png" />{events.map(e=><CircleMarker key={e.id} center={[e.venue.latitude,e.venue.longitude]} radius={10} pathOptions={{color:colors[e.monitoringPriority],fillColor:colors[e.monitoringPriority],fillOpacity:.75}}><Popup><strong>{e.name}</strong><br/>{e.venue.name}<br/>{e.venue.city}, {e.venue.country}<br/>Next date: {e.eventDates[0].startDate}<br/>Confidence level: {e.confidence}<br/><Link href={`/events/${e.id}/`}>Open event page</Link></Popup></CircleMarker>)}</MapContainer>}
