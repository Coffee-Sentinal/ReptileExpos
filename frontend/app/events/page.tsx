import {api} from '@/lib/api'; import {EventCard} from '@/components/EventCard';
export default async function Events(){const events=await api.events(); return <><h1 className="text-3xl font-bold">Events</h1><p className="mb-4 text-slate-300">Search/filter foundation: country, date range, risk level, confidence, taxon group, CITES appendix, website type, and active monitoring window.</p><div className="card mb-4 grid-auto"><input className="rounded bg-navy p-2" placeholder="Search events (MVP visual filter)"/><select className="rounded bg-navy p-2"><option>Risk level</option></select><select className="rounded bg-navy p-2"><option>Confidence</option></select></div><div className="grid-auto">{events.map(e=><EventCard key={e.id} event={e}/>)}</div></>}
